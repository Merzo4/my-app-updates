name: Trigger and monitor MerzoStream 0.1.0n release

# rerun: 1
on:
  push:
    branches: [main]
    paths: ['.github/workflows/trigger-merzostream-010n.yml']

permissions:
  actions: write
  contents: write

jobs:
  trigger-monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 65
    steps:
      - name: Dispatch exact 0.1.0n SHA and capture result
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          REPO="$GITHUB_REPOSITORY"
          WORKFLOW="merzostream-010n-release.yml"
          BRANCH="merzostream-010n-release"
          TARGET_SHA=$(gh api "repos/$REPO/branches/$BRANCH" --jq '.commit.sha')
          echo "Target 0.1.0n SHA: $TARGET_SHA"
          gh workflow run "$WORKFLOW" --repo "$REPO" --ref "$BRANCH"
          echo "0.1.0n release dispatched"
          RUN_ID=""
          for n in $(seq 1 50); do
            RUN_ID=$(gh run list --repo "$REPO" --workflow "$WORKFLOW" --branch "$BRANCH" --event workflow_dispatch --limit 20 --json databaseId,headSha,createdAt --jq ".[] | select(.headSha == \"$TARGET_SHA\") | .databaseId" | head -n1)
            if [ -n "$RUN_ID" ]; then break; fi
            sleep 5
          done
          if [ -z "$RUN_ID" ]; then
            printf '{"status":"monitor_error","targetSha":"%s","message":"exact release run not found"}\n' "$TARGET_SHA" > /tmp/010n-status.json
            : > /tmp/010n-last-failure.log
          else
            set +e
            gh run watch "$RUN_ID" --repo "$REPO" --exit-status --interval 5
            WATCH_RC=$?
            set -e
            gh run view "$RUN_ID" --repo "$REPO" --json databaseId,status,conclusion,headSha,url,workflowName > /tmp/010n-status.json
            if [ "$WATCH_RC" -ne 0 ]; then
              gh run view "$RUN_ID" --repo "$REPO" --log-failed > /tmp/010n-last-failure.log || true
            else
              : > /tmp/010n-last-failure.log
            fi
          fi
          upload_file() {
            local local_path="$1" repo_path="$2" msg="$3"
            local content sha
            content=$(base64 -w0 "$local_path")
            sha=$(gh api "repos/$REPO/contents/$repo_path?ref=main" --jq '.sha' 2>/dev/null || true)
            if [ -n "$sha" ]; then
              gh api --method PUT "repos/$REPO/contents/$repo_path" -f message="$msg" -f content="$content" -f branch=main -f sha="$sha" >/dev/null
            else
              gh api --method PUT "repos/$REPO/contents/$repo_path" -f message="$msg" -f content="$content" -f branch=main >/dev/null
            fi
          }
          upload_file /tmp/010n-status.json merzostream/ci/010n-ci-status.json "Record MerzoStream 0.1.0n CI status"
          upload_file /tmp/010n-last-failure.log merzostream/ci/010n-last-failure.log "Record MerzoStream 0.1.0n CI failure log"
          cat /tmp/010n-status.json
