# Merzo Optimizer Local Lab Evidence

This isolated branch contains only Local Test Center evidence. It intentionally has no `.github/workflows` directory, so evidence pushes do not trigger GitHub Actions.

Canonical latest report: `LOCAL_LAB_EVIDENCE/LATEST/`.
